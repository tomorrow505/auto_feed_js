# TODO (Legacy Wiki Parity)

本清单只记录“对照旧版 Wiki 后，重构版仍缺失/不一致”的部分，避免把已实现内容混进来。

旧版 Wiki 参考（页面名）：
`设置页面`、`源种子页面`、`转载功能`、`查重及快搜`、`图片转存`、`图片处理`、`Ptgen功能`、`简化MediaInfo`、`禁转提示`、`中文显示`、`一键签到`、`qb推送`、`清洗种子-自动下载发布的种子`、以及各类“转载到X/适配X”页面。

## P0: 转发主链路 (源站解析 -> 缓存 -> 目标站预填)
- [ ] PTP 作为源站：严格按旧版规则清洗标题、处理多种子链接（必须识别 `torrentid` 并绑定到对应种子），并补齐“Edition 信息/Scene/字幕替代”等关键字段
- [ ] PTP 作为目标站：补齐所有必填与常见可选项的自动勾选策略（类型、来源、容器、编码、分辨率、音频、字幕、是否可替代等）
- [ ] HDB 作为源站：描述提取去噪（避免抓到无关图片/空 synopsis），并补齐 BDInfo/MediaInfo 抽取与清洗
- [ ] HDB 作为目标站：补齐 Category/Codec/Resolution/Audio/Team/Anonymous 等选择项自动填充
- [ ] CHD/CHDBits 作为源/目标：预填选项与标题清洗完全对齐旧版（并支持默认域名为 `chdbits.co`，设置中可切换）
- [ ] GZ 架构“组内多种子”：只在“具体种子”页面显示转发入口（类似旧版 “需要 permalink / torrentid 才出现” 的行为）
- [ ] 目标站打开后自动预填的稳定性：页面刷新、SPA 跳转、慢加载情况下不丢数据

## P0: 种子清洗与种子文件链路
- [ ] “清洗种子”字段策略对齐旧版：comment/source/created by/creation date 处理，多 tracker 场景、announce 替换规则一致
- [ ] 内站转内站：统一替换 announce 为“无个人 passkey”的安全 tracker（旧版逻辑）
- [ ] 外站转外站：按目标站 upload 页提示自动替换 announce，并确保不落盘保存
- [ ] “发布后自动下载种子”站点补齐（旧版支持的站点清单与行为对齐）

## P1: “点击获取”外站中文信息 (豆瓣/ptgen)
- [ ] “剧集非第一季”场景：按旧版交互实现“检索名称 -> 选择结果 -> 获取”链路（旧版会展开隐藏 textarea/下拉结果）
- [ ] IMDb -> 豆瓣 ID 两种模式的细节对齐：API 与页面爬取的失败回退、频控/验证码提示
- [ ] PTGen 三种来源的对齐：`api.iyuu.cn`、`ptgen`、`豆瓣页面爬取` 的输出格式与填充字段一致
- [ ] “在源站点击获取后写回缓存”：确保目标站预填能拿到副标题/简介/中文名等信息

## P1: 查重/快搜 (旧版: 查重与发布切换 + 自定义快搜模板)
- [ ] “查重/发布”两模式的行为对齐旧版：查重依据（IMDB/英文名）与展示位置
- [ ] 快搜模板管理对齐旧版：一行一个模板、变量替换 `{imdbid}/{imdbno}/{search_name}`、模板导入（旧版 temple_search_urls）
- [ ] 快搜 UI 对齐：按钮紧凑排版、超出自动换行、颜色可读

## P1: 禁转提示 (旧版: 关键字提示 + 站点规则)
- [ ] 禁转/拒转关键字提示补齐，并对齐旧版的“二次确认”交互
- [ ] 站点规则提示补齐：FRDS/丐帮/OB/南北洋/瓷器等典型规则（旧版 Wiki 列表）

## P1: 图片转存与图片处理 (旧版: 图片转存 + 大窗口处理)
- [ ] “防盗链图床清单”覆盖对齐旧版：HDB/HDC/TTG/Lemon/AVZ/PHD/FL 等常见来源的原图提取规则
- [ ] “多图转 PIXhost”限制与策略对齐旧版：只对指定来源图床生效，输出 350px BBCode
- [ ] “拉取图像跳转上传”对齐旧版：img.hdbits/imgbox/hostik 等页面的一键拉取按钮挂载位置与兼容性
- [ ] 图片处理大窗口行为对齐旧版：获取大图、字符串替换、图片提取、对比图设置、抽取展示图等（旧版 `图片处理`、`转载至海豹`）

## P1: 设置页面能力 (旧版: 站点勾选/常用站点/匿名/排序/导入导出)
- [ ] 站点启用勾选与“常用站点”联动规则对齐旧版：常用站点列表必须是启用站点的子集
- [ ] 站点排序对齐旧版：可拖拽调整显示顺序
- [ ] 配置导入/导出指引与能力（旧版通过油猴存储面板备份）
- [ ] 新站点无需改代码的“默认站点配置”能力（旧版 default_site_info 的思路，仅限 NP 架构）

## P2: 远程推送 (旧版: qb 推送)
- [ ] qb 推送交互对齐旧版：tag->路径映射、跳检默认值、提示文案、失败原因可读化
- [ ] 兼容旧版 JSON 配置格式（多机器、多 tag、多路径）
- [ ] 旧版仅支持 qb 的行为差异梳理，并补齐 Transmission/Deluge 的文档与提示

## P2: 外站中文显示 (旧版: PTP/HDB/FL/AVZ/Tik/KG/NBL/CG)
- [ ] 旧版“中文显示”覆盖站点补齐，并做到可在设置中逐项开关
- [ ] PTP/HDB 豆瓣信息面板的样式与宽度对齐（避免超出、错位、背景冲突）

## P3: 其他旧版功能 (未纳入主链路但需要对齐)
- [ ] 一键签到：迁移旧版的“选择站点 -> 一键签到/状态色”页面与逻辑
- [ ] 简化 MediaInfo：迁移旧版“长 -> 短”转换工具入口与输出格式
- [ ] 皇后/OpenCD、电子书 bibliotik、BTN、海豹(GPW) 等专题适配页的能力逐项补齐或明确标记“不计划支持”
